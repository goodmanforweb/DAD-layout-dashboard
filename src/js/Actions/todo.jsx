
import { createAction } from 'redux-actions';

export const editTitle = createAction('edit title');
export const editDataSource = createAction('edit dataSource');
export const editChartProperty = createAction('edit chartProperty');
export const editChartParameter = createAction('edit chartParameter');
